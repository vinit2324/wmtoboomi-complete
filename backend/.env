# Server Configuration
BACKEND_PORT=7201
BACKEND_HOST=localhost

# MongoDB Configuration
MONGODB_URL=mongodb+srv://vinit:Delhi123@vvmdb1.6umwxkw.mongodb.net/wmtoboomi?retryWrites=true&w=majority&appName=VVMDB1
DATABASE_NAME=wmtoboomi

# Security - Generate a new Fernet key for production
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-key-here-generate-new-one

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=524288000

# CORS Origins
CORS_ORIGINS=http://localhost:7200,http://127.0.0.1:7200
